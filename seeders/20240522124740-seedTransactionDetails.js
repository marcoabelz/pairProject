"use strict";

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    /**
     * Add seed commands here.
     *
     * Example:
     * await queryInterface.bulkInsert('People', [{
     *   name: 'John Doe',
     *   isBetaMember: false
     * }], {});
     */
    let datas = require("../data/transactionDetail.json").map((transaction) => {
      transaction.createdAt = transaction.updatedAt = new Date();
      return transaction;
    });
    await queryInterface.bulkInsert("TransactionDetails", datas);
  },

  async down(queryInterface, Sequelize) {
    /**
     * Add commands to revert seed here.
     *
     * Example:
     * await queryInterface.bulkDelete('People', null, {});
     */
    await queryInterface.bulkDelete("TransactionDetails", null)
  },
};
